{
  "name": "hearts-game-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Hearts Game application",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:hearts": "playwright test hearts-game-e2e.spec.js",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
